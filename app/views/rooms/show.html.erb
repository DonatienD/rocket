<h3><strong>Niveau : </strong><%= @room.level %> </h3>
<h3><strong>Identifiant de la classe : </strong><%= @room.id %> </h3>


<% @chapters.each do |chapter| %>
<strong><%= chapter.name.capitalize %></strong>
  <% chapter.missions.each do |mission| %>
            <%= link_to(mission_path(mission)) do %>
              <strong><%= mission.name.capitalize %></strong>
            <% end %>
  <% end %>
<% end %>
